{
  "cli": {
    "version": ">= 12.6.2",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "android": {
        "buildType": "apk"  // Specify APK for development builds
      }
    },
    "preview": {
      "distribution": "internal",
      "android": {
        "buildType": "apk"  // Specify APK for preview builds
      }
    },
    "production": {
      "autoIncrement": true,
      "android": {
        "buildType": "apk"  // Specify APK for production builds
      }
    }
  },
  "submit": {
    "production": {}
  }
}
